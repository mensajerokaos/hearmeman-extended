# =============================================================================
# Media Analysis Database Configuration
# =============================================================================
# This is the NEW independent database for media analysis service
# Separate from AF infrastructure (af_memory)
# =============================================================================

# Database connection (new media_analysis database)
MEDIA_DB_HOST=media-pg-1
MEDIA_DB_PORT=5432
MEDIA_DB_NAME=media_analysis
MEDIA_DB_USER=media_analysis_user
MEDIA_DB_PASSWORD=media_analysis_secure_pwd_2026

# Full URL override (optional - takes precedence over individual settings)
# MEDIA_DATABASE_URL=postgresql+asyncpg://media_analysis_user:password@media-pg-1:5432/media_analysis

# =============================================================================
# Legacy AF Database Configuration (fallback)
# =============================================================================
# Set USE_AF_DATABASE=true to use the legacy AF database instead
# USE_AF_DATABASE=false
# AF_DB_HOST=af-postgres-1
# AF_DB_PORT=5432
# AF_DB_NAME=af-memory
# AF_DB_USER=n8n
# AF_DB_PASSWORD=

# =============================================================================
# Connection Pool Settings
# =============================================================================
DB_POOL_MIN=5
DB_POOL_MAX=20
DB_POOL_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=1800

# =============================================================================
# Debugging
# =============================================================================
# Enable SQL echo for debugging (set to true to see all SQL queries)
DB_ECHO=false
LOG_LEVEL=INFO
